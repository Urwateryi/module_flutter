name: module_flutter
description: A new flutter module project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  dio: ^2.1.13
  json_annotation: ^3.0.0
  pull_to_refresh: ^1.5.4
  rxdart: ^0.22.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.6.6
  json_serializable: ^3.2.0

flutter:

  uses-material-design: true

  module:
    androidX: true
    androidPackage: com.example.module_flutter
    iosBundleIdentifier: com.example.moduleFlutter
